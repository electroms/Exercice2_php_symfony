{% extends 'base.html.twig' %}

{% block title %}Annonce{% endblock %}

{% block body %}
    <h1>Annonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ advert.id }}</td>
            </tr>
            <tr>
                <th>Non</th>
                <td>{{ advert.name }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ advert.brandId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('advert_index') }}">Retour à la liste</a>

    <a href="{{ path('user_show', {'id': advert.id}) }}">Réserver</a>

    {{ include('advert/_delete_form.html.twig') }}
{% endblock %}
